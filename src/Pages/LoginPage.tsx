import React from "react";
import Login from "../Components/Login";

const LoginPage = () => {
  return (
    <div className="w-[100vw] h-[100vh] bg-blue-100 flex items-center justify-center p-10">
      <Login />
    </div>
  );
};

export default LoginPage;
